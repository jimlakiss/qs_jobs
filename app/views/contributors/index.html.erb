
<h1 class="mb-4">
  Contributors
  <% if params[:contributor_type].present? %>
    – <%= params[:contributor_type].humanize %>
  <% end %>
</h1>

<%= link_to "New Contributor", new_contributor_path, class: "btn btn-primary mb-3" %>

<table class="table table-hover">
  <thead class="table-light">
    <tr>
      <th>Company</th>
      <th>Key Contact</th>
      <th>Email</th>
      <th>Phone</th>
       <th>Contributor Type</th>
    </tr>
  </thead>

  <tbody>
    <% @contributors.each do |contributor| %>
      <tr>
        <td>
          <%= link_to contributor.company_name, contributor_path(contributor) %>
        </td>
        <td><%= contributor.key_contact %></td>
        <td><%= contributor.email %></td>
        <td><%= contributor.phone_number %></td>
        <td><%= contributor.contributor_type %></td>
      </tr>
    <% end %>
  </tbody>
</table>